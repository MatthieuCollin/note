<form method="post" action="{{ path('app_ajout_controle_delete', {'id': controle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ controle.id) }}">
    <button class="btn">Delete</button>
</form>
