{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Eleve</th>
                {% for eleve in eleves%}
                <td>{{ eleve.lastname }}</td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ajout_classe_index') }}">back to list</a>

    <a href="{{ path('app_ajout_classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('ajout_classe/_delete_form.html.twig') }}
{% endblock %}
